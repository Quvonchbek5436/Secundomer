import styled from 'styled-components'
import colors from './Color'

const StyleSecundomer = styled.div`
      span { color:${colors.textColor}
      
        width:300px;
        height:500px;
      
      .clock-holder{
        background-color:${colors.bgColor} !important;
`

export default StyleSecundomer